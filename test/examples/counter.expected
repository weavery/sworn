// Generated by Sworn

clarity.requireVersion("0.0")

function getCounter(state) {
  let result = null
  result = clarity.ok(state.counter)
  return result
}

function increment(state) {
  let result = null
  state.counter = (state.counter + 1)
  result = clarity.ok(state.counter)
  return state
}

function decrement(state) {
  let result = null
  state.counter = (state.counter - 1)
  result = clarity.ok(state.counter)
  return state
}

export function handle(state, action) {
  const input = action.input
  if (input.function === 'getCounter') {
    return {result: getCounter(state)}
  }
  if (input.function === 'increment') {
    return {state: increment(state)}
  }
  if (input.function === 'decrement') {
    return {state: decrement(state)}
  }
  return {state}
}
