// Generated by Sworn

clarity.requireVersion("0.0")

function read(state) {
  let result = null
  result = clarity.ok(state.sender)
  return result
}

function update(state) {
  let result = null
  state.sender = clarity.some(clarity.txSender())
  result = clarity.ok(state.sender)
  return state
}

function clear(state) {
  let result = null
  state.sender = null
  result = clarity.ok(state.sender)
  return state
}

export function handle(state, action) {
  const input = action.input
  if (input.function === 'read') {
    return {result: read(state)}
  }
  if (input.function === 'update') {
    return {state: update(state)}
  }
  if (input.function === 'clear') {
    return {state: clear(state)}
  }
  return {state}
}
