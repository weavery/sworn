// Generated by Sworn

clarity.requireVersion("0.1")

function sayHi(state) {
  return {state, result: clarity.ok("hello world")};
}

function echoNumber(state, val) {
  return {state, result: clarity.ok(val)};
}

export function handle(state, action) {
  const input = action.input;
  if (input.function === 'sayHi') {
    return sayHi(state);
  }
  if (input.function === 'echoNumber') {
    return echoNumber(state);
  }
  return {state};
}
