// Generated by Sworn

clarity.requireVersion("0.1")

function read(state) {
  return clarity.ok(state.height);
}

function update(state) {
  state.height = clarity.blockHeight();
  return {state, result: clarity.ok(state.height)};
}

export function handle(state, action) {
  const input = action.input;
  if (input.function === 'read') {
    return {result: read(state)};
  }
  if (input.function === 'update') {
    return update(state);
  }
  return {state};
}
